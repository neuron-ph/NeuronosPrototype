# Ticket Board

Internal tracking for Neuron OS. Claude is the primary reader/writer — Marcus drops tickets here, Claude keeps status current.

**Status:** `Todo` · `In Progress` · `Blocked` · `Review` · `Done`
**Priority:** `P0` critical · `P1` high · `P2` normal · `P3` low

---

## Active

| ID | Title | Area | Priority | Status | Notes |
|----|-------|------|----------|--------|-------|
| NEU-006 | Booking create blocked for Pricing — RLS `ops_bookings:create` gap (permission half) | Pricing / Bookings · RBAC | P0 | Done (prod 6/12) | **Permission half of the original booking bug (split-brain project link moved to NEU-013, 6/04).** `bookings_insert` WITH CHECK = `current_user_has_module_permission('ops_bookings','create')`; on PROD, Pricing staff & manager both resolve false → INSERT denied → generic "Failed to create booking." Root: `ops_bookings` is a hidden module whose grant is DERIVED client-side (`deriveHiddenModuleGrants` = OR of per-service creates) but the DB stores a STALE/missing copy, and the INSERT policy checks ONLY the umbrella key while SELECT/UPDATE accept any per-service `ops_*` key. Also: the Create button is gated by hardcoded `canPerformBookingAction` dept list (`["Pricing","Operations"]`), not the Access Profile → button shows but DB denies. This is the **Phase-0 permission slice of NEU-012** (RBAC consolidation). Fix: broaden `bookings_insert` to accept per-service create keys (mirror SELECT/UPDATE); persist+backfill derived `ops_bookings:create`; grant Pricing baseline booking-create (per Marcus); drive the button off `can()`; set `created_by`; surface the real error. **FIXED on dev (Phase 1, 6/04):** root cause was the umbrella `ops_bookings:create` being derived in the browser but never persisted, so the DB INSERT policy (which checks it) denied. Migration `137_resolve_hidden_grants_at_write` makes the hidden-module derivation permanent (trigger on access_profiles + permission_overrides) + backfills — `ops_bookings:create` now persists wherever a per-service create exists (Pricing has marine/others). `ProjectBookingsTab` Create button now gates on `can('ops_bookings','create')` (the same key RLS checks) instead of `canPerformBookingAction` dept allowlist. Verified on dev: Pricing user → true, HR/BD/Accounting → false. NOT on prod yet (awaiting release). **Re-reported 6/08 by Falcons Pricing ("cannot create booking, save-as-draft also fails").** ⚠️ **CORRECTION (6/11): the fix IS on prod.** Verified live: migrations 136/137/141/142-145/146 all applied to prod; the live `bookings_insert` WITH CHECK on prod = `current_user_can_act_on_booking('create')` (new helper, not the old umbrella); real Falcons Pricing users (jr.pricing01-06, jr.manager03, jr.supervisor06) resolve `create=true` via `ops_marine_insurance:create`+`ops_others:create`(+`ops_projects_bookings_tab:create`). So RLS allows their booking inserts on prod TODAY — the earlier "not on prod yet" note was stale. If the 6/08 failure persists it is NOT this RLS gap: either the report predates the 6/06-09 release, or it's a DIFFERENT error (likely NEU-013 409 split-brain, or the Create button still gating on the retired `ops_bookings` umbrella key client-side). **Needs a fresh live repro of the exact error before any further action.** Side note: Pricing lacks `ops_forwarding/brokerage/trucking:create` — helper is per-action not per-service, so a Pricing-created Forwarding booking passes DB but isn't explicitly granted; decide intentionally. **6/12: ISOLATED into its own fix session per Marcus** — paired with NEU-030 (the project/booking-tab RBAC cluster). Still needs a fresh live repro of the exact on-screen error (button greyed vs red toast + text, and which service) before any code change; prod RLS already verified to allow Falcons Pricing inserts, so if it still fails it's NOT this gap (likely the client-side Create button still gating on a retired key, or a missing project/contract container per NEU-015). **6/12 — partly explained by NEU-030: for the NULL-access-profile users (`jr.supervisor06`, `jr.pricing06`) booking-create fails for the SAME reason the tabs are blank — no profile → RLS `current_user_can_act_on_booking('create')` resolves false. The profiled Pricing users (`jr.pricing01/02/04/05`, `jr.manager03`) DO resolve create=true. So WHICH user hit "cannot create booking" decides the cause: if it's Genevieve/pricing06 → fixed by the NEU-030 profile assignment; if it's pricing01-05 → it's the client-side gate / container issue. Still need: which user + exact on-screen error.** **6/12 LIVE PROBE (dev, read-only RLS simulation as Genevieve `39bb11c2`): a Forwarding booking INSERT + readback on the orphaned PRJ-504417 SUCCEEDS** — after the NEU-030 cascade fix she resolves `ops_marine_insurance:create`+`ops_others:create`=true, so `current_user_can_act_on_booking('create')` passes; `bookings.customer_id` is nullable (FK `on delete set null`) so a null customer doesn't FK-violate; the SELECT-after-insert RLS readback returns the row. **So RBAC/RLS/DB is NO LONGER the blocker.** Traced the full submit path (`CreateForwardingBookingPanel.handleSubmit:165-225`): insert → `persistAssignmentsForNewBooking` (no-ops `{ok:true}` when no assignments) → `logCreation` (fire-and-forget). The console errors are all NON-fatal: contract RPC 400 (fixed in NEU-007/migration 199; was caught → []), `activity_log` FK 409 (post-insert, not awaited), and `ConsigneePicker` `resolve_by_name` returning **undefined** for the deleted AF PHILIPPINES customer (React-Query throw → disabled consignee picker — a NEU-026 orphan symptom, minor bug: should return null). **No hard blocker found in the core path for a project-container booking.** Two real fixes landed that the failing screenshots predate (NEU-030 grant cascade + migration 199 contract RPC). **NEXT: Marcus to hard-refresh dev and retry; if it STILL fails, capture the exact red toast text + the Network-tab failing request AFTER clicking Create (the one missing fact), and retry on a HEALTHY project (not the orphaned ALL IN CIF) to isolate the NEU-026 poison.** **ROOT CAUSE FOUND + FIXED on dev 6/12 — it was NOT the booking insert (that always passed); it was the ASSIGNMENT WRITE.** Live probe surfaced the real error: `replace_booking_assignments_atomic is restricted to Operations/Executive`. The RPC had a HARDCODED `get_my_department() IN ('Operations','Executive')` identity-gate (the exact NEU-012 anti-pattern). Flow: booking inserts fine (capability OK) → `persistAssignmentsForNewBooking` → `replace_booking_assignments_atomic` → **department gate rejects Pricing** → `CreateForwardingBookingPanel:221` deletes the just-created booking and throws → generic "Failed to create booking." Only bit when assignments were present (healthy customer w/ a team default); the orphaned AF PHILIPPINES had no assignment defaults so it no-op'd past this (its failure was the separate NEU-026 consignee/resolve symptom). **Fix (migration `200`): re-gate the RPC on the booking ACTION capability — `current_user_can_act_on_booking('create'|'edit') OR is_executive()` — mirroring the insert/RLS, so Pricing (and any booking-create grantee) can write assignments. Verified on dev via full RLS probe as Genevieve: booking insert + assignment write BOTH succeed end-to-end ("FULL_FLOW_OK"). NOT on prod (prod has the old department gate → ship 200 with release). Likely the SAME blocker for the broader Falcons "cannot create booking" reports — any Pricing user creating a booking with a team/assignment default.** **SECOND (CLIENT-SIDE) BLOCKER found 6/12 after the DB fix — this was the "nothing happens on click, no error, no console" symptom.** Each `Create*BookingPanel` `handleSubmit`/`handleSaveDraft` opens with a SERVICE-SPECIFIC backstop, e.g. `CreateForwardingBookingPanel:167` `if (!can("ops_forwarding","create") && !can("ops_forwarding","edit")) return;`. The DB helper `current_user_can_act_on_booking` is per-ACTION (ORs ALL services), so Genevieve passes RLS via `ops_marine_insurance/others:create`=true — but she has `ops_forwarding:create/edit`=FALSE, so the client backstop `return`s SILENTLY (no toast, no log) → dead Create button. Classic NEU-012 client-gate drift: client checks a NARROWER key than RLS. Confirmed her grants: fwd_create=false, brk_create=false, marine_create=true, others_create=true. **Fix (frontend, dev): new `src/utils/bookings/bookingCapability.ts` `canCreateOrEditBooking(can)` ORs create|edit across all 6 booking modules (mirrors the DB helper); all 10 backstops across the 5 panels (Forwarding/Brokerage/Trucking/MarineInsurance/Others, submit+save-draft) repointed to it. tsc 159 (zero new).** ⚠️ FRONTEND change — takes effect on dev only after git push → Vercel redeploy (the DB migrations 198/199/200/201 are already live on the shared dev DB; this code is not until pushed). **CORRECTION 6/12 (Marcus) — the first capability fix was based on the WRONG principle.** It ORed the incidental service modules (ops_marine_insurance/others), so Genevieve passed only by accident, NOT via the doctrine grant. Per "While in [Module], they can [Action] [Record]", her real authority is **`pricing_projects_bookings_tab:create`** ("While in Projects, Create Bookings") = confirmed true. BOTH the client helper AND the DB RLS helper `current_user_can_act_on_booking` ignored the per-door Bookings-tab grants — they only checked the service modules + `ops_projects_bookings_tab`. **Corrected fix:** `bookingCapability.ts` + migration `202` now OR the action across EVERY Bookings record surface — the per-door Projects/Contracts bookings tabs (`bd/pricing/ops/acct_projects_bookings_tab`, `bd/pricing/acct_contracts_bookings_tab`) + the 5 service modules. **Proven on dev:** with `ops_marine_insurance/others:create` stripped in a rolled-back probe, `current_user_can_act_on_booking('create')` STILL returns true for Genevieve — authorised purely by `pricing_projects_bookings_tab:create`. DB migration 202 live on dev; client helper change needs the push. **RELEASED TO PROD 6/12 (stable/2026-06-12, main @ f5d1a4e):** client helper committed+pushed (dev f241190 → main); migrations 200 + 202 applied to prod via apply_migration. Verified on prod — `current_user_can_act_on_booking` honors the per-door bookings tabs; rolled-back probe proves Genevieve is authorised to create bookings purely by `pricing_projects_bookings_tab:create` (marine/others stripped → still true). Done on prod+dev. |
| NEU-013 | Booking create: split-brain `project_id` vs `linked_bookings` → 409 / phantom bookings | Ops / Pricing · Bookings | P1 | Done (prod 6/11) | **Split from NEU-006 (6/04) — standalone create-flow data bug, NOT RBAC; ships independently of NEU-012.** Two project↔booking linkages disagree: `bookings.project_id` (guarded by unique idx `bookings_one_per_service_per_project`, migration 110) vs `projects.linked_bookings` JSONB (what `ProjectBookingsTab` shows + its "already booked" guard). `CreateForwardingBookingPanel.handleSubmit` sets `project_id` only `if (fetchedProject)`: the from-project path (prefill, `fetchedProject` null) inserts `project_id = NULL` then links via the array → always succeeds, never enforces the rule; the Forwarding-module path stamps `project_id` → 409 collision with any existing `project_id` Forwarding booking. CONFIRMED on PROD console (`409 … bookings_one_per_service_per_project`); the blocker was phantom `FWD202606-006` (project_id set, NOT in linked_bookings → invisible/undeletable in UI) — **already deleted on prod 6/04** to unblock PRJ-614474. Fix: always set `bookings.project_id` in project context (resolve from `fetchedProject?.id` OR `formState.project_number`) across all 5 `Create*BookingPanel`s; keep `linked_bookings` in sync (or make `ProjectBookingsTab` read by `project_id`); friendly `23505` toast w/ link to the existing booking. **IMPLEMENTED (Option 2, dev) 6/04:** `bookings.project_id` is now the single source of truth; `projects.linked_bookings` retired (column kept, unread). Writers `linkBookingToProject`/`unlinkBookingFromProject` (`projectAutofill.ts`) now set/clear `bookings.project_id` + expose `isProjectBookingConflict`. Readers `ProjectBookingsTab` + `ProjectBillings` query `bookings.eq(project_id)` (orphan-cleanup hack removed). Conflict handling: `CreateBookingFromProjectPanel` deletes the just-created row + shows "already exists" on link conflict; `CreateForwardingBookingPanel` catch translates 23505 → friendly msg. Migration `136_backfill_booking_project_id.sql` backfills project_id from the legacy array (unambiguous + non-conflicting only; dupes left unlinked) — **applied to dev, NOT prod**. Typecheck clean on touched files; dev integrity verified (no project has 2 same-service bookings). TODO before close: release to prod (code + migration 136); follow-ups = drop `projects.linked_bookings` column after prod verify, retire vestigial `quotations.linked_bookings` on contract side (same pattern). **Bundled into the 6/08 re-report of NEU-006 — ship both to prod together.** |
| NEU-007 | "No active contract found" for Brokerage team though active on Jayson's account | Pricing / Contracts | P1 | Done (prod 6/11) | Reported 6/04. Brokerage team sees "No active contract found", but the contract shows as activated on Sir Jayson's account. Likely a team/ownership-scope mismatch on contract lookup. **Re-confirmed open 6/08.** Related but distinct from NEU-015 (project shows "no active contracts" in the booking picker). **DIAGNOSED 6/11 — it's a REGRESSION from migration 185.** Path: `ContractDetectionBanner.tsx:161` shows the warning when `fetchActiveContractsForCustomer` (`contractLookup.ts:38-43`, a plain `from('quotations')` select) returns `[]`. The row is filtered by the live `quotations_select` RLS (migration 185). Two branches both fail for a Brokerage user looking at a Pricing-owned contract: **Branch A** `current_user_can_view_record('contracts', owner)` fails because the Brokerage `contracts` dial is `own`/`team` and the owner is cross-department (Pricing); **Branch B** (relationship gate) only grants read if a `bookings.contract_id`/`projects.quotation_id` link already exists — but during *detection* the booking isn't created yet → chicken-and-egg. Migration 134 originally allowed any ops-module holder to read Active/Expiring contracts; 184 kept a broad `can_act_on_booking('view')` read; **185 replaced that with the relationship gate to plug a list-scan leak (BD rep seeing all contracts+rates)** — which also killed legitimate pre-booking detection. **Proposed fix (decision needed):** (a) *recommended* — a customer-scoped `SECURITY DEFINER` RPC `detect_active_contracts_for_customer(name)` that requires an ops/contract view grant and returns only Active/Expiring contracts for that **specific** customer (header fields, not rate matrices); repoint `contractLookup.ts:38` to it. Satisfies detection without reopening the 185 leak. (b) simpler but worse — widen ops profiles' `contracts` dial to `everything` (walks back 185's intent, re-exposes the full contract list). **DONE on dev 6/11 (option a):** migration `196_contract_detection_rpc.sql` adds two `SECURITY DEFINER` RPCs gated by `current_user_can_detect_contracts()` (the same view-capability OR-set as 185 branch A): `detect_active_contracts_for_customer(name)` — customer-scoped, returns header fields only (id/number/name/status/validity/services + computed pol/pod), **no rate matrices**, so it can't dump rate cards; and `get_contract_for_booking(id)` — by-id full fetch (incl. rates) for the one contract the user selects (the booking panels call `fetchFullContract` pre-link, which was also RLS-blocked). `contractLookup.ts` repointed from the raw `quotations` select to these RPCs. Caught + fixed `quotations.id` being **text** not uuid. No capability → no rows, so 185's list-scan leak stays closed. build clean, tsc 159. NOT on prod. Follow-up tuning option (Marcus's call): the rate-bearing `get_contract_for_booking` currently accepts the broad OR-set incl. `bd_inquiries`/`bd_contracts` view — could tighten to booking actors only if BD browse-keys shouldn't load rates. **BUG FOUND + FIXED on dev 6/12 (migration `199`): migration 196's `detect_active_contracts_for_customer` selected `q.contract_validity_start`/`q.contract_validity_end`, which don't exist — the real columns are `contract_start_date`/`contract_end_date`. The RPC 400'd on EVERY booking-panel load ("column q.contract_validity_start does not exist"), spamming the console and breaking contract detection. 199 corrects the column refs (output names unchanged). **Then a SECOND latent bug surfaced (revealed once columns were fixed): `COALESCE types text[] and jsonb cannot be matched` — `quotations.services` is `text[]` (udt `_text`), not jsonb, but the RPC did `coalesce(q.services,'[]'::jsonb)` and returns `services jsonb`. Fixed in migration `201` with `to_jsonb(coalesce(q.services,'{}'::text[]))`.** Verified on dev (as Genevieve): RPC returns rows for "A PLUS FALCONS FREIGHT INC." with no error. **RELEASED TO PROD 6/12 (stable/2026-06-12):** migrations 199 + 201 applied to prod via apply_migration; verified the RPC runs clean on prod (no column/type error). Done on prod+dev.** |
| NEU-008 | Make currency configurable via Profiling | Pricing / Profiling | P2 | Done (prod 6/11) | Reported 6/04. Request: move currency list into Profiling so new/different currencies can be added there (manage like other profiling lookups). Foundation for NEU-009/010/011. **Re-confirmed open 6/08.** Confirmed: currency lists are hardcoded inline (no lookup table) in ≥4 places — `ChargeCategoriesManager.tsx:236-239`, `BuyingPriceSectionV2.tsx:744-749`, `VendorsSection.tsx:735-739` (all USD/PHP/EUR/CNY) and `shared/pricing/UniversalPricingRow.tsx:394-397` (PHP/USD only — the NEU-009 selling restriction). Moving to a profiling lookup would centralize all four and auto-fix NEU-009. **DONE on dev 6/11:** the `currencies` master table (from NEU-027) is now manageable in Admin → Profiling → System → **Currencies** (new `CurrenciesTab` — add/edit/activate/reorder via SidePanel; PHP guarded as functional, can't deactivate). Retired the remaining hardcoded currency dropdowns to source from `useCurrencies()`: `UniversalPricingRow`, `BuyingPriceSectionV2`, `VendorsSection`, `ChargeCategoriesManager`, `NewJournalEntryScreen`, `CollectionCreatorPanel`, `AddRequestForPaymentPanel` (CurrencyPill). Adding a currency in Profiling now appears everywhere with no code change. tsc 159 (zero new), build clean. NOT on prod. |
| NEU-009 | Selling currency limited to PHP/USD (buying has 4) | Pricing / Quotations | P1 | Done (prod 6/11) | Reported 6/04. Buying side exposes 4 currencies; selling side only PHP and USD. Make all currencies available on both buying and selling. **Re-confirmed open 6/08 + root cause found:** selling-side currencies live in `src/components/shared/pricing/UniversalPricingRow.tsx:394-397` (PHP, USD only), used by `SellingPriceSection`; buying-side `BuyingPriceSectionV2.tsx:744-749` has USD/PHP/EUR/CNY. Quick fix = add EUR/CNY to UniversalPricingRow; proper fix = source from the NEU-008 profiling lookup so both sides share one list. **DONE on dev 6/11:** added EUR/CNY to `UniversalPricingRow.tsx:395` so selling matches buying (PHP/USD/EUR/CNY); typecheck + build clean. Left `ContractRateCardV2.tsx:388` (contract rate matrix, typed `"PHP"|"USD"`) for a separate consistency pass. NOT on prod. |
| NEU-010 | Vendor section currency reverts to USD on save | Pricing / Quotations | P1 | Done (prod 6/11) | Reported 6/04. Currency chosen in the Vendors section of a quotation reverts to USD after saving. Make the vendor currency changeable and persist the selection. **Re-reported 6/08 — but code read does NOT reproduce a reset-on-save:** `VendorsSection.tsx:718` reads `vendorCurrencies.get(vendor.id) || "USD"` (USD is just the per-vendor default) and updates via `onCurrencyChange` (986-989); no save handler forces USD. ⚠️ **Needs live repro to pinpoint.** Most likely the selection isn't being **rehydrated on reload** (saved currency not read back into `vendorCurrencies` when the quotation is reopened) rather than overwritten on save — i.e. a load/persist-roundtrip gap, not a reset. Confirm with Marcus / live test which exact step "reverts to USD". **DIAGNOSED 6/11 — confirmed a persist/rehydrate gap, and worse than thought: the entire vendors array is NEVER persisted to the quotation.** (1) The `Vendor` type (`types/pricing.ts:51-63`) has **no `currency` field** — per-vendor currency lives only in in-memory `vendorCurrencies: Map` (`VendorsSection.tsx:107`), read as `.get(id) || "USD"`. (2) SAVE: the payload built at `QuotationBuilderV3.tsx:2199-2273` (passed to `onSave`) has **no `vendors:` key** — vendors never get written. (3) LOAD: `vendors` initializes to `[]` (`QuotationBuilderV3.tsx:315`) and is never seeded from `initialData`. (4) DB confirms: `quotations.vendors` column exists but is empty on every dev row. So on reopen the Map is empty → currency falls back to USD. **Shares one root cause with NEU-011.** **Proposed fix (ready to build):** add `currency?: string` to `Vendor`; persist `vendors` in the save payload (column exists); rehydrate `vendors` from `initialData` on load + in the viewMode sync effect (`:460-487`). **DONE on dev 6/11 (ships with NEU-011):** added `currency?` to the `Vendor` type; `VendorsSection` now stores per-vendor currency on the vendor object (`getVendorCurrency`/`setVendorCurrency` — Map stays as the in-session override but reads fall back to saved `vendor.currency`); `QuotationBuilderV3` seeds `vendors` from `initialData`, rehydrates it in the viewMode sync, and includes `vendors` in the save payload; `vendors` added to the `pricing` JSONB whitelist in both `Pricing.tsx` (`PRICING_KEYS`) and `BusinessDevelopment.tsx` so it round-trips (load already spreads `pricing` to top-level). build clean, tsc 159. NOT on prod. |
| NEU-011 | Vendor stays "International Partners" after choosing another partner | Pricing / Vendors | P1 | Done (prod 6/11) | Reported 6/04. Vendor field stays "International Partners" even when another partner (e.g. "All In Partners") is selected. Selection not persisting/displaying. **Re-reported 6/08 — but code read does NOT reproduce:** `PartnerSheet.tsx:48` defaults `partner_type:"international"` but rehydrates from `initialData` on edit (81-146); `VendorsSection.tsx:91` uses "International Partners" only as the default for a NEW vendor row and reads `vendor.partner_type` once a vendor is picked (361). ⚠️ **Needs live repro.** Likely same shape as NEU-010 — a persist/rehydrate roundtrip gap (saved partner type not read back), or the label simply not re-rendering after selection. Pin down the exact step before fixing. **DIAGNOSED 6/11 — same root cause as NEU-010: the vendors array is never persisted/rehydrated with the quotation.** On reopen, `newVendorType` resets to the hardcoded default `"International Partners"` (`VendorsSection.tsx:93`) because nothing restores the saved vendor `type`. Secondary in-session bug: picking a partner in `SearchableFormSelect` only sets `newVendorName` (`:535`) and never reconciles `newVendorType` to the chosen partner's `partner_type`, so the Type label can stay "International Partners" even mid-session. (The partner *master* editor `PartnerSheet`→`useNetworkPartners` round-trips `partner_type` correctly — this bug is specific to the in-quotation VendorsSection.) **Proposed fix (ready to build, ships with NEU-010):** persist + rehydrate the vendors array (incl. each vendor's `type`); additionally set `newVendorType` from the selected option's `partner_type` in `SearchableFormSelect.onChange` (`:533-540`). **DONE on dev 6/11:** the load-bearing fix — persisting + rehydrating the vendors array (incl. each vendor's `type`) — is implemented with NEU-010, so a reopened quotation restores the saved vendor type instead of defaulting to "International Partners". (Deferred the optional in-session `newVendorType`↔selected-partner reconciliation as a minor polish; the persist/rehydrate roundtrip resolves the reported bug.) build clean, tsc 159. NOT on prod. |

| NEU-014 | Error-message engine — one humanizer for all failure toasts | UX · Infra | P2 | Todo | **Enhancement, not a bug.** Today 425 `toast.error(...)` across 113 files are either black-box ("Failed to X. Please try again." — throws the real reason away) or raw-dump (`${error.message}` — leaks Postgres jargon to forwarders). Same scattered-truth anti-pattern as NEU-012/NEU-013: "what does failure X mean" is decided in 425 places, with bespoke one-off translators (`duplicateMblMawbMessage`, `isProjectBookingConflict`) drifting apart. Supabase errors already carry the answer (`code`/SQLSTATE, `details`, `constraint`, `hint`) — we discard it. Plan: single `humanizeError(err, ctx)` + a small registry keyed on **constraint name** (e.g. `bookings_one_per_service_per_project` → "A Forwarding booking already exists for this project") and bare SQLSTATE for generics (`23505` dup, `23503` referenced-row-deleted, `23502` required-field-missing). Two layers: known → curated sentence; unknown → honest fallback with a short **reference code** + full error logged (no "Please try again" lie, no jargon). Special case: RLS `42501` carries no reason by design → engine needs a call-site `ctx` hint (`{action, module}`) to say "You don't have permission to {action}. Ask an admin to grant it in your Access Profile." — legible band-aid over the NEU-006/NEU-012 RBAC gap, not a cure. Scope discipline (focus over scope): build engine once, wire only high-traffic write paths first (booking-create, quotation/contract save, e-voucher, billing — ~15–20 sites); rest keep working + get honest fallback when routed. Open decisions: (1) reference codes worth it vs noise (lean yes); (2) first pass = booking-create only vs whole write set. Awaiting Go Ahead. |
| NEU-012 | RBAC consolidation — make Access Profile the single source of truth | RBAC · Infra | P1 | Todo | Discovered while diagnosing NEU-006. Two parallel permission systems disagree: (A) Access Profile (DB `module_grants` + RLS `current_user_has_module_permission` + client `PermissionProvider.can`) — the intended truth; (B) hardcoded `src/utils/permissions.ts` (`canAccessModule`, `canPerform{Quotation,Project,Booking,EV}Action`, `canDeleteEVoucher`) + ~35 inline `department === …` / ~20 `role === …` checks. Client gates are usually MORE lenient than DB → "button shows, save fails" (the booking bug). Systemic root = same permission defined twice and drifting: `ops_bookings:create` derived client-side (`deriveHiddenModuleGrants`) vs stored-in-DB (stale); `visibility_scope` hardcoded in client (`roleDefaultVisibilityScope`) AND SQL (`current_user_visibility_scope`). Hotspots: booking-create, full E-Voucher approval workflow (`canPerformEVAction`, `evoucherApproval.ts`, `dashboardFetchers.ts`), module-access gates (`ContractsModule`/`ProjectsModule`/`Pricing`/`BusinessDevelopment`). Plan: P0 = NEU-006 (booking-create permission slice; split-brain project link is the separate NEU-013, ships independently of this); P1 canonical `can()` + shim permissions.ts; P2 module gates; P3 EV workflow→grants; P4 visibility-scope unification; P5 structural dept branches; P6 decommission + lint guard. Principles: one derivation rule persisted to DB; every client gate backed by RLS; no new hardcoded dept/role permission checks. Awaiting Go Ahead + 3 decisions (Pricing baseline create scope; EV-as-grants vs fixed workflow; shim vs rip-replace). **Implementation plan written 6/04: `docs/RBAC_PRINCIPLE_IMPLEMENTATION_PLAN.md`** — principle locked; live prod audit found 3 fronts (~20 grant / ~28 legacy / wide-open incl. `projects`/`transactions`/`accounts`/catalog); resolver does literal lookup while client derives umbrella (root of NEU-006); 4 invariants; phased ledger-burndown (Phase 0 read-only audit workflow → Phase 1 resolver+NEU-006 slice → Phase 2 DB gaps → Phase 3 retire identity gates → Phase 4 regression guard); slice = DB+app+verify-agree, gated per change. |

| NEU-015 | Project booking: "Project / Contract Number" → unified Project **+** Contract search | Ops / Pricing · Bookings | P2 | Done (prod 6/11) | Reported 6/05; re-reported live 6/10 (Falcons, Brokerage "ALL IN CIF" from project PRJ-584417 → field showed "No active contracts found", panel demanded a contract, Create greyed). Root cause: the 5 `Create*BookingPanel`s reused the Operations contract-only flow — the `project_number` field override searched **contracts only** and creation was gated on `detectedContractId`, so a project-launched booking couldn't see its own project and couldn't be created (also wrongly blocked spot-priced All-Inclusive/Non-Regular). **IMPLEMENTED on dev 6/11 (Marcus's design):** new shared `ProjectContractPicker` (`operations/shared/`) searches **both** active projects + active contracts for the customer in one dropdown; whichever is picked becomes the container (`bookings.project_id` OR `contract_id`). A container is **required** — either kind, but you cannot create without one (replaces the contract-only gate). When launched from a project the project is pre-selected (`projectContext` threaded through `CreateBookingFromProjectPanel` → all 5 panels). `ContractDetectionBanner` + the contract-only overrides removed; contract POL/POD + delivery-option constraint loading moved into the picker's onChange; Forwarding's project-pick autofill preserved. New `fetchProjectsForCustomer` util. Typecheck clean. NOT on prod. |
| NEU-016 | Consignee picker disabled ("Select a customer first") on project booking despite customer linked | Ops / Pricing · Bookings | P2 | Done (prod 6/11) | Reported 6/05; visible again in the 6/10 repro. Root cause confirmed: `BookingConsigneePickerAdapter` reads `ctx.customer_id`/`ctx.customer_name`, but `useBookingFormState` built the context via `getSelectedCustomer(formState)` **without** the panel's known `customerId` prop — so in the project flow (where `formState.customer_name` is a plain string with no id) the picker never got a customer id and stayed disabled. **FIXED on dev 6/11:** `useBookingFormState` now takes `{ fallbackCustomerId }` and threads it into the context; all 5 panels pass their `customerId` (= `project.customer_id`). Consignee enables + loads the customer's consignees in the project flow. Typecheck clean. NOT on prod. |
| NEU-017 | Write affordances (Edit/Save/Create/Delete) shown to view-only users | RBAC · UX | P2 | Review (dev) | Reported 6/05 (testing NEU-012 Contract #6 record visibility as Carolina, a Customs-Declarant-style profile — view-only on Contacts/Customers). The record detail view still renders the **Edit** button and editable form with Save/Cancel; the save only fails at the database (RLS denies the UPDATE), surfacing the raw PostgREST error "Cannot coerce the result to a single JSON object" (`.single()` on 0 returned rows). **NOT a security hole** — RLS already blocks the write, data is safe — this is affordance correctness + UX. Two parts: (1) gate Edit/Create/Delete/Save buttons on the same grant RLS checks (`can(module,'edit'|'create'|'delete')`) so view-only users never see the option; audit record detail views (contacts, customers, bookings, projects, quotations, e-vouchers, financials, etc.). (2) the jargon error → fold into NEU-014's humanizer (RLS `42501` / empty-result ⇒ "You don't have permission to edit this"). This is the **write-side / app-gate slice of NEU-012** (record visibility = read side is done; this is the write affordances). **Part 1 DONE on dev (6/06):** system-wide gating shipped during the NEU-012 push; verified by the 5-agent strict sweep — residue closed same day (Group A: ungated Record Collection across 7 surfaces, customer New Inquiry, e-voucher New Request, catalog category popover side-door, inbox assign on a view key, UserDetailPage edit/EV-authority/danger-zone on exec_users:view-only route). Remaining residue = participation surfaces → split to **NEU-018**. Part 2 (jargon RLS error → friendly message) folds into **NEU-014** as originally noted. Accepted nit: /admin/users/new reachable by direct URL with exec_users:view only — create is server-enforced (edge fn), entry button gated. Close when the release ships. |
| NEU-018 | Gating sweep: participation surfaces — comments, attachments, inbox compose | RBAC · UX | P2 | Todo | Queued 6/06 from the strict-principle write-affordance sweep (post-NEU-017 audit; Group B). Three surface families today run on "view = can participate" — implied capability, which violates strict the same way role fallbacks did: (1) **CommentsTab** (`shared/CommentsTab.tsx`) — anyone who can view any Comments tab can post + attach; zero `can()` checks. Surfaces: contacts, customers, contracts, quotations, booking comments. (2) **EntityAttachmentsTab** (`shared/EntityAttachmentsTab.tsx`) — anyone who can view any Attachments tab can **upload and delete** files. Surfaces: contacts, customers, contracts, quotations, projects. (3) **Inbox compose/reply/close** (`inbox/ComposePanel.tsx`, `ComposeBox`, `ThreadDetailPanel` close) — ungated; assign was the only gated action and was fixed to `inbox:edit` in the 6/06 sweep. Fix shape: gate post/upload with the surface's `*_comments_tab`/`*_attachments_tab` `:create` key and delete with `:delete` (thread per-surface props like Teams tabs' `canEdit`); decide inbox compose key (`inbox:edit`, or bless open-compose as a deliberate messaging-surface exception — Marcus's call); flip the matching `actionApplicability.ts` entries so the knobs un-dash; ideally match RLS on `comments`/attachment tables (today INSERT is open to authenticated). **Blast-radius check required first** (same method as the 6/06 collections fix): these knobs were dead during profile curation, so create/delete grants may need seeding into curated profiles before gating — nobody should lose comment/upload ability they use daily. NOT release-blocking: current behavior is the long-standing model and record visibility is RLS-protected; this is affordance honesty (strict: no capability without a knob). |
| NEU-019 | "Consignee" tab/label → "Consignee/Shipper" (full rename incl. Customer Profile/BD) | Pricing / Ops · Bookings | P3 | Fixed (dev) | Reported 6/08 (Falcons Pricing). Client wants the **Add Consignee** tab/label renamed to "Consignee/Shipper" (one field serves consignee on import + shipper on export — the schema comment at `bookingScreenSchema.ts:161-165` confirms these were deliberately consolidated into one `details.consignee` field). **Display-only rename — keep the data field `consignee`.** Label appears at: `src/config/booking/bookingScreenSchema.ts:106` (the shared GI field label, drives all 5 services), `src/components/projects/ProjectBookingReadOnlyView.tsx:600/692/758`, `src/utils/documents/invoiceDocumentResolver.ts:297`, `src/components/shared/ConsigneePicker.tsx:45` (placeholder), `src/components/bd/ConsigneesTab.tsx:106/116`, `src/components/bd/ConsigneeInlineSection.tsx:148/156/328`. **Open Q:** rename everywhere (incl. the BD Consignees module + invoice doc) or only the booking/quotation GI label the client saw? Recommend: booking GI label + the quotation consignee tab the client referenced; leave the standalone BD Consignees module as-is unless asked. Trivial once scope is set. **DONE on dev 6/11:** renamed the shared GI consignee field label to "Consignee/Shipper" (`bookingScreenSchema.ts:106`) — drives the field + picker placeholder across all 5 booking services and the quotation flow. Data key stays `consignee`. Left `ProjectBookingReadOnlyView` untouched (Consignee + Shipper are deliberately separate columns there) and the BD Consignees management module as-is. NOT on prod. **RE-SCOPED 6/12 (Marcus): the original fix only touched the shared booking GI label — client confirms they meant the CUSTOMER PROFILE (BD) module, and "genuinely all the consignee labels should just be turned to Consignee/Shipper."** Reopening to cover the labels the 6/11 pass deliberately skipped: `ConsigneesTab.tsx` (106 header, 116/274/302 buttons, 128 form title, 139 field label), `ConsigneeInlineSection.tsx` (148/156/328), `ConsigneePicker.tsx:45` placeholder, `invoiceDocumentResolver.ts:297` (printed invoice — client-facing, confirm before changing). **DELIBERATELY EXCLUDE `ProjectBookingReadOnlyView.tsx` (600/692/758)** — there Consignee and Shipper are separate side-by-side columns, so renaming one to "Consignee/Shipper" next to a real "Shipper" column would be wrong. Display-only; data key stays `consignee`. **6/12 status:** the original booking-GI-label rename (`bookingScreenSchema.ts:106`) is now ON PROD (shipped with stable/2026-06-12 dev→main merge). **The RE-SCOPED part — renaming the Customer Profile (BD) consignee labels — was NOT coded yet** (deferred when the session pivoted to the NEU-006 booking blocker). So this stays OPEN for the BD-module + invoice-PDF labels. Ready to do on auto. **BD-RENAME DONE on dev 6/12 (full auto):** renamed all Customer-Profile/BD consignee labels → "Consignee/Shipper" — `ConsigneesTab.tsx` (header "Consignees / Shippers", "Add Consignee/Shipper" ×2, "New/Edit Consignee/Shipper", "Consignee/Shipper Name *", "No consignees / shippers yet"), `ConsigneeInlineSection.tsx` (section labels + "Add consignee/shipper..."), `ConsigneePicker.tsx:45` placeholder, and the **invoice PDF** label `invoiceDocumentResolver.ts:297` (Marcus OK'd client-facing). Excluded `ProjectBookingReadOnlyView` (separate Consignee/Shipper columns). Data key stays `consignee`. tsc 159 (zero new). NOT on prod (frontend — ships next release). |
| NEU-020 | Inquiries dashboard total shows PHP for a USD quotation | Pricing / Quotations | P1 | Done (prod 6/11) | Reported 6/08 (Falcons Pricing). A USD proposal shows its **total in PHP on the inquiries dashboard**, but the **printed PDF correctly shows USD** (PDF reads `quotation.currency` via `quotationDocumentResolver.ts:1114`). Confirmed root cause of a PHP hardcode: `src/components/pricing/quotations/FinancialSummaryPanel.tsx:81` hardcodes the header `"FINANCIAL SUMMARY (PHP)"` and lines 48-49 call `formatMoney(amount, "PHP")` — ignoring the `currency` prop that's already passed in. Note: the main quotations list (`QuotationsListWithFilters.tsx:235`) **already** renders `{item.currency} {total}` correctly. **Open Q (blocks the fix):** the client said "inquiries dashboard" specifically — need to pinpoint that exact surface (is it FinancialSummaryPanel inside the builder, or a separate Inquiries list/summary that also hardcodes PHP?). Once the surface is confirmed, fix = format with `quotation.currency` instead of literal PHP. **DIAGNOSED 6/11 — surface confirmed:** the **inquiry detail view** (`/bd/inquiries/:id` → `QuotationDetail` → … → `QuotationBuilderV3` in viewMode), whose totals render through the shared `FinancialSummaryPanel`. `QuotationBuilderV3.tsx:2811` passes the correct `currency` prop in, but the panel **ignores it and hardcodes PHP**: `FinancialSummaryPanel.tsx:48` `formatMoney(amount,"PHP")` (used by every total row incl. grand total at :280), plus literal labels at `:81` "(PHP)", `:227` "Other Charges (PHP)", `:272` "TOTAL (PHP)". The `currency` prop is already in scope. `FinancialSummaryPanel` is imported ONLY by `QuotationBuilderV3`, so the 4-line fix is central — fixes both the editor and the inquiry detail. The inquiries **list** (`QuotationsListWithFilters`) is already correct. **DONE on dev 6/11:** `FinancialSummaryPanel` now formats every total with the document `currency` (`docCurrency`) instead of literal PHP, and the header / "Other Charges" / "TOTAL" labels reflect it; removed the now-contradictory "↻ {currency} → PHP" badge and gated the secondary "APPROX. TOTAL (USD)" reference + USD rate input to PHP documents only (a foreign-currency doc shows its own currency; the PDF shows no approx block). Single shared component → fixes both the builder and inquiry detail. build clean, tsc 159. NOT on prod. |
| NEU-021 | Inbox notifications late for routed/assigned inquiries | Inbox · Infra | P1 | Done (prod 6/11) | Reported 6/08 (Falcons Pricing). Routed/assigned inquiries appear in the assignee's inbox with a perceptible delay. Root cause: **the inbox has no realtime subscription and no polling.** `src/hooks/useInbox.ts:65-222` (threads) and `224-268` (counts) use `staleTime: 0` but never auto-refetch — `refresh()` (lines 270-272) is only called on manual actions (send message, close/reopen). By contrast `src/hooks/useNotifications.ts:89-129` DOES subscribe to Supabase realtime (sidebar badge updates instantly). **Found a second, deeper cause: the ticket tables were never in the `supabase_realtime` publication** (only `notification_*` were) — so even a perfect subscription would no-op. **DONE on dev 6/11:** migration `195_inbox_realtime.sql` publishes `tickets`/`ticket_participants`/`ticket_messages`/`ticket_read_receipts` (idempotent); `useInbox()` now opens a realtime channel on those tables and invalidates the inbox queries on any change (mirrors `useNotifications`). Routed/assigned items now appear live. tsc 159, build clean. NOT on prod (needs migration 195 + frontend on release). |
| NEU-022 | Amend converted quotation (manager-gated, two flows) | Pricing / Quotations | P2 | In Progress | Reported 6/08 (Falcons Pricing): Pricing keeps needing to re-edit quotations after conversion, for BOTH contract rates and project rates. Current lock: `src/utils/quotationStatus.ts:124-133` `isQuotationLocked()` true when `project_id`/`project_number` set, status "Converted to Project"/"Converted to Contract", or `contract_status==="Active"`; Edit hidden at `QuotationFileView.tsx:1148`. **DESIGN LOCKED 6/12 (Marcus).** Reframe: a single "unlock" is wrong — the two paths are wired differently downstream (traced 6/12). Project/spot billings read LIVE from `quotation.selling_price` for un-invoiced items; contracts are SNAPSHOTS (frozen at booking-create, versioned via migration 120 `contract_rate_versions`). Anything already invoiced/posted to accounting stays frozen on BOTH paths (hard safety rail). **Decisions:** (1) **Amend** = a new grantable capability in Access Configuration (`pricing_quotations:amend` / `pricing_contracts:amend`), given to **managers** — NOT reused `edit`. **Implemented as a dedicated 7th action verb `amend`** (first cut wrongly reused `approve`; Marcus pushed back 6/12 — `approve` is a legit future quotation-approval gate and must stay free). `amend` applicable ONLY on `pricing_quotations` + `pricing_contracts` (inert "—" elsewhere, like `export`); added to `ActionId`/`PERM_ACTIONS`, both `ACTION_LABELS` maps, matrix `GRID_COLS`, cascade-gen ACTIONS, confirm-gated like approve. (2) **No reason field, no sign-off** — a manager with the grant amends and saves. (3) **Project flow:** Amend → edit prices → un-invoiced charges update + re-sync `projects.details` snapshot; invoiced charges frozen. (4) **Contract flow:** Amend → save creates a new rate version → **live re-rate**: EVERY still-open booking's UN-INVOICED, contract-sourced billing lines re-rate to the new rate; invoiced lines frozen (a booking can end up mixed — correct). Boundary = **Option A "unbilled = new rate"**; the **effective-date concept is DROPPED** (no date gating; version history kept only as a record). (5) **Manual overrides untouched** by re-rate + get a visible **"Manually adjusted"** badge. **VERIFICATION (6/12):** manual-adjust tracking is only PARTIAL today — `billing_line_items.source_type` tags `contract_rate` vs `manual` (migration `015`), but editing a `contract_rate` line's amount does NOT flip the tag (`UnifiedBillingsTab.tsx:438-476,592` silently overwrites) → can't distinguish edited from untouched. So an explicit `is_manually_adjusted` flag must be added (Phase 0). **BUILD PLAN (order 0→1→2→3):** **P0 Foundations** — (0a) add `pricing_quotations:amend` to `ACCESS_SCHEMA` (`accessSchema.ts`) + regen cascade edges (new migration like `198`, via `scripts/genGrantCascadeMigration.ts`), grant to managers; (0b) migration add `billing_line_items.is_manually_adjusted BOOLEAN DEFAULT false`, set true in `UnifiedBillingsTab.handleItemChange` when amount/unit_price edited on a `contract_rate` line, persist through `mapRow` + `send_billing_items_to_booking` RPC. **P1 Project Edit Rates** — `QuotationFileView`: when locked AND `can('pricing_quotations','amend')`, show **Amend** button opening `QuotationBuilderV3` edit mode + guardrail banner; on save persist `selling_price` + re-sync `projects.details.charge_categories`. Verify the live-read assumption first (`UnifiedBillingsTab.tsx:180-250`). **P2 Contract Amend + re-rate engine** — Amend opens `ContractRateCardV2`; save creates new `contract_rate_versions` row; a `SECURITY DEFINER` RPC finds open bookings under the contract → their unbilled `source_type='contract_rate' AND is_manually_adjusted=false` lines → re-rates via `contractRateEngine`, skips invoiced/paid/voided + manual lines, re-points booking `rate_version_id`. **Policy defaults (Marcus OK'd):** vanished rate rows left as-is (not auto-deleted); brand-new charge rows NOT auto-injected into existing in-flight bookings. **P3 Indicator** — "Manually adjusted" badge in `UnifiedBillingsTab` rows. Cortex: decisions `ca487b23` + `e3ba8089`. **STATUS 6/13:** P0+P1+P2 done on dev. **P2 SCOPE CHANGED (Marcus 6/13):** the "live re-rate of existing billings" was DROPPED — reason: contract billing is generated INTERACTIVELY (operator enters quantities/mode/selections/permits in RateCalculationSheet; the saved billing_line_items keep only the result, not the rate-row provenance/mode), so a silent retroactive re-rate would have to GUESS inputs and could corrupt GL-feeding amounts. Instead: **contract amend → new rate version (existing `createRateVersion`) → re-point every LIVE booking (status NOT IN Completed/Cancelled) to the new version via SECURITY DEFINER RPC `repoint_live_bookings_to_rate_version` (migration 204, gated `current_user_has_module_permission('pricing_contracts','amend')`)** so the booking's RATE CALCULATOR produces the amended rates going forward. Existing billings untouched. Contract amend is surfaced in the **Contracts module** (`ContractDetailView` → `ProjectOverviewTab` Unlock, gated `pricing_contracts:amend`; `handleSaveContractQuotation` already created the version, now also re-points + reports count). Unlock UX = the `🔒 Locked ▾` dropdown IS the control (`UnlockAmendButton`, surface-accurate `note`). NOT on prod (migrations 203+204 + frontend). **P3 DONE 6/13 + extended to projects (Marcus surfaced the gap):** project billings are REFLECTIVE — `UnifiedBillingsTab` overwrites every unbilled line's amount with the live quotation value on each render, so a hand-edit to a quotation-derived line was silently clobbered. Now: (1) `handleItemChange` flags `is_manually_adjusted` on ANY source-derived line (contract_rate OR quotation_item / has source_quotation_item_id), not just contract lines; (2) the reflective merge SKIPS overwriting `is_manually_adjusted` unbilled lines — a manual override sticks and the quotation/amendment leaves it alone; (3) "Manually adjusted" badge added to `BillingsTable.BreakdownSubRow` (the existing rate-metadata sub-row → no change to the shared `UniversalPricingRow`), shows on both project + contract lines. Flag loads via `useContainerFinancials` `select('*')`. tsc 154 (zero new), build clean. **ALL OF NEU-022 (P0–P3) DONE ON DEV. NOT on prod** — release = migrations 203 + 204 + frontend (dev→main). Cortex: + `941a115f`. **6/15 (Marcus re-reported "unlock to edit quotation once routed/completed" → "Let's fully fix this"):** this IS NEU-022 (`UnlockAmendButton` at `QuotationFileView.tsx:925-927`). Why it's not visible yet for him on prod: (a) the whole feature is built on dev but **NOT released to prod**, and (b) it's gated `isLocked && canAmendRates && quotation_type !== "contract"` — needs the `pricing_quotations:amend` / `pricing_contracts:amend` grant assigned to the right managers, and contracts are hard-excluded by that `!== "contract"` guard (contract path surfaced separately via Contracts module per the P2 re-scope). **To fully close: release P0–P3 to prod (migrations 198/203/204 already noted + frontend), grant `amend` to managers, verify both the quotation and contract unlock paths.** |
| NEU-023 | Show total **buying** amount in the quotations module table | Pricing / Quotations | P2 | Done (prod 6/11) | Reported 6/08 (Falcons Pricing). Enhancement: surface the **total buying amount** alongside the selling total on the inquiries & quotations dashboards. Data exists — `quotation.buying_price?: BuyingPriceCategory[]` (`types/pricing.ts:291`) is persisted; buying total = `buying_price.reduce((s,c)=>s+c.subtotal,0)`. Today `QuotationsListWithFilters.tsx:218-238` shows only the selling "Total" column (`financial_summary.grand_total`). Fix: add a Buying Total column (and likely Margin %) to the list, formatted in the quotation's currency (see NEU-020). **Open Q:** should buying figures be visibility-gated (margin is sensitive — should Sales/BD see vendor cost on the dashboard, or only Pricing/Exec)? Recommend gating behind a Pricing/Exec capability. **Clarified 6/11: not the dashboard — the quotations *module table* (the list).** **DONE on dev 6/11:** added a "Buy: {currency} {total}" sub-line under the selling total in each `QuotationsListWithFilters` row, using the same line-derived display currency as the selling total (`deriveLineDisplay`). **Decision (Marcus): NOT gated — shown to everyone** (I proposed gating to vendor-viewers; Marcus said don't gate). build clean, tsc 159. NOT on prod. |
| NEU-024 | Quotation line shows an amount even when quantity is 0 | Pricing / Quotations | P1 | **Done (prod) 6/15 — re-fix** | Reported 6/08 (Falcons Pricing). A line item with quantity = 0 still shows a non-zero amount. Root cause: zero is coerced to 1. `UnifiedLineItemRow.tsx:130` parsed qty as `parseFloat(e.target.value) || 1` (0 falsy → 1); `LineItemRow.tsx:78` calc used `(item.quantity || 1)`. **DONE on dev 6/11:** UnifiedLineItemRow qty onChange now keeps a real 0 (NaN→0, no `|| 1`) + `min="0"`; LineItemRow calc uses `(item.quantity ?? 1)` so 0 stays 0 (undefined still defaults 1). Decision taken: qty 0 → ₱0 (matches the report), not blocked. Typecheck + build clean. **⚠️ REOPENED 6/15 (Marcus re-reported, "fully fix this") — the 6/11 fix landed on the WRONG components.** `UnifiedLineItemRow.tsx` is an ORPHAN (imported nowhere) and `LineItemRow.tsx` is not the live quotation row. The component actually rendered by the builder is `src/components/shared/pricing/UniversalPricingRow.tsx` (via `SellingPriceSection`), which still does `(data.quantity \|\| 1)` at **:456, :514, :523**; buying side same bug at `BuyingPriceSectionV2.tsx:799` `(item.quantity \|\| 1)`. Display-only (persisted amount already correct — `SellingPriceSection.tsx:107,446` use `?? 1`). **Fix: `\|\| 1` → `?? 1` for quantity at those 4 spots.** **DONE on dev 6/15:** `UniversalPricingRow.tsx` 514/523 → `(data.quantity ?? 1)`; the 456 spot was inside the per-line ≈₱ marker removed by NEU-032 (so `phpEquivalent`/`rate` deleted, one fewer coercion); `BuyingPriceSectionV2.tsx:799` → `(item.quantity ?? 1)`. tsc 154 (zero new), build clean. **RELEASED TO PROD 6/15 (stable/2026-06-15-3, main @ a61b7a2).** **⚠️ INCOMPLETE — the print/PDF path was still broken (Marcus caught it 6/15).** The client-facing PDF re-derives line amounts in `quotationDocumentResolver.ts`: `effectiveItemAmount` (88-93) treated a stored `amount` of *exactly 0* as "unset" → recomputed `unitPrice × (quantity || 1)` = unit price, AND `subtotal += convertedAmt` summed it into the printed subtotal + grand total (real total corruption, not display). **FIXED on dev 6/15:** `quotationDocumentResolver.ts` 93/98/777 `(quantity || 1)` → `(quantity ?? 1)` (the `!== 0` guard self-corrects since the recompute now yields 0 for qty 0). tsc 154 (zero new), build clean, resolver tests pass (2 unrelated NEU-020 "(PHP)" label assertions stale). **RELEASED TO PROD 6/15 (stable/2026-06-15-4, main @ 458a954).** Fully done on prod+dev. |
| NEU-025 | Replace Neuron bank details with A Plus Falcons in printed PDF | Pricing / Documents | P1 | Blocked (info) | Reported 6/08 (Falcons Pricing). Printed quotation PDF shows **Neuron** bank details; should show **A Plus Falcons**. **Not a code bug** — bank details are already data-driven: `PrintableDocumentPdf.tsx:659-684` renders from `company_settings` (resolved in `quotationDocumentResolver.ts:1241-1256`, with a per-document `bankOverride` option). The Neuron values are the fallback defaults in `src/hooks/useCompanySettings.ts:30-32` (`bank_name: "BDO Unibank"`, `bank_account_name: "Neuron Logistics Inc."`, `bank_account_number: "0012-3456-7890"`). Fix = a **data change**: update the `company_settings` row (id='default') on dev (and later prod) with A Plus Falcons' bank name / account name / account number — no rendering code change. **Blocked on Marcus providing the actual A Plus Falcons bank details** ("will provide in awhile"). **6/15: Marcus re-reported as "remove the Neuron bank details in the print PDF" and said "wait on this."** So: STILL BLOCKED — decide whether to (a) clear the prod `company_settings` bank fields so nothing prints, (b) default `showBankDetails=false` (`documentSettings.ts:53,118,158`), or (c) wait for the real A Plus Falcons details and swap them in. Prod still carries placeholder Neuron values (`bank_name='BDO Unibank'`, `bank_account_name='Neuron Logistics Inc.'`, `bank_account_number='0012-3456-7890'`); they print by default on quotation + invoice PDFs. Holding per Marcus. |

| NEU-026 | Customer deletion silently orphans live records (no delete guard) | Pricing / BD · Data Integrity | P1 | Guard live (dev+prod) | Found 6/11 while tracing why Falcons project PRJ-504417 (ALL IN CIF / AF PHILIPPINES) had a null customer, no consignees, and a dead consignee/shipper picker. **Root cause: customer deletion is unguarded.** Every FK → `customers` is `ON DELETE SET NULL` (quotations, projects, bookings, invoices, collections, evouchers, contacts) or `ON DELETE CASCADE` (consignees, customer_attachments, assignment_profiles). So deleting a customer with live records **silently orphans them** (customer_id nulled, only denormalized `customer_name` text left) and **cascade-deletes its consignees**. Audit log: AF PHILIPPINES was a real customer created 2026-05-22 (Rovilyn), **deleted 2026-05-25 by Mark Javier as part of a 34-customer purge** — which orphaned its quotation+project and destroyed its consignees (the direct reason the booking consignee picker was empty — rows were deleted, not hidden). Blast radius: **14 of 136 quotations now have null customer_id.** Verified real prod data (dev is a clone) → orphans exist on prod too. **Systemic fix DONE on dev (migration `193_guard_customer_delete.sql`):** `BEFORE DELETE` trigger on `customers` (SECURITY DEFINER so the integrity count ignores caller RLS) that BLOCKS deletion when the customer still has quotations/projects/bookings/invoices/collections/e-vouchers, with a human message listing the counts — enforced at the DB so it holds for every path (UI/REST/MCP), surfaces via the existing customer-delete `alert`. Ancillary owned records (contacts/consignees/tasks/activities/attachments) intentionally don't block. Verified on dev: blocks MORRISON EXPRESS (23 linked) with nothing deleted; clean customer still deletes. **RELEASED TO PROD 6/11** (migration 193 applied to prod; trigger attached + enabled, count verified non-destructively — auto-classifier correctly blocks a real prod delete test). Guard now live on dev+prod. **Remaining TODO:** (1) decide the `consignees` CASCADE (keep, or also guard); (2) heal the 14 existing orphans (re-link or write off) — separate data cleanup; junk = TEST/OBII CAKES/BB CAKES/ROVILYN/ALLYSPHERE (write off), real = AF PHILIPPINES (PRJ-504417 + QUO2605220222), PRIORITEX LOGISTICS, DAN INTERNATIONAL, Garden Barn — needs Marcus's call on recreate-vs-writeoff since source customer details were deleted and consignees were CASCADE-destroyed (unrecoverable); (3) optional UX: nicer toast + pre-delete dependent count instead of raw `alert`. |
| NEU-027 | Full multi-currency support (PHP+USD → N currencies, incl. EUR/CNY) | Accounting · Pricing · Infra | P1 | Done (prod 6/11) | Decided 6/11 (Marcus). Engine is already currency-agnostic at the core (math `amount×rate→PHP`, dual-currency journal lines, shared FX gain/loss accts 4510/7010/4530/7030, `resolveExchangeRate`); USD is just the only currency *switched on*. **Decisions locked:** (1) **Option B — full foreign currency** (real EUR/CNY invoices, AR/AP, collections, month-end revaluation), not quote-only; (2) **flexible/generic** currency model (adding a currency later = data/config, not code — folds in NEU-008 "currencies in Profiling"); (3) **rates handled as today — per-document manual entry** at posting time (`exchange_rates` table is the optional prefill via `resolveExchangeRate`; no rate-management UI built now). **Audit blockers (4-agent read-only, 6/11):** HARD — 5 DB CHECK constraints `IN ('PHP','USD')` (`079` accounts.currency, `080` exchange_rates pair, `081`×5 doc base_currency, `082` journal base_currency); `SUPPORTED_ACCOUNTING_CURRENCIES`+`CURRENCY_LOCALES` (`accountingCurrency.ts:16/121`) + local `CURRENCIES` in `AccountSidePanel:18`; FX revaluation USD-hardcoded (`useFxRevaluation.ts:34/45/96/117/125`, `fxRevaluation.ts:24` literal type, idempotency key global-per-date not per-currency); 2-option dropdowns in `InvoiceGLPostingSheet:833`, `CollectionGLPostingSheet:989`, `GeneralJournal:587/927`. COSMETIC — symbol ternaries `=== "USD" ? "$" : "₱"` (`AddRequestForPaymentPanel:1943`, `AccountSidePanel:267/283`) + `account.currency === "USD"` foreign-checks (`AccountLedger:82/244`) → use `currencyGlyph`/`!== FUNCTIONAL_CURRENCY`; `ChartOfAccounts:209` type narrowing. **Pre-existing gap exposed:** quote→billing→invoice rate chain is broken even for USD — `rateCardToBilling.ts:235` hardcodes `forex_rate:1` and the quotation's user-typed `forex_rate` never propagates to billing/invoice/GL; multi-currency forces fixing this. **Phased (dev-first, prod in steps w/ explicit go):** P1 foundation (widen 5 constraints + allowlist/locales, generic currency source) → P2 display+forms (glyphs, dropdowns off the list, foreign-checks) → P3 quote→GL rate propagation (fixes USD gap too) → P4 generalize FX revaluation (loop non-PHP currencies, per-currency rate + idempotency) → P5 test full EUR cycle quote→invoice→collection→GL→revaluation. Touches the GL — do not rush; foreign bank/settlement accounts seeded only where a client actually holds that currency. **PROGRESS (dev): P1 ✅ (6/11, migration 194 `currencies` master + FK gate + accountingCurrency opened — EUR/CNY no longer coerced to PHP); P2 ✅ (6/11, `useCurrencies` hook → data-driven currency dropdowns across Invoice/Collection GL sheets, Journal filter, CoA account-create + ledger; generalized `=== "USD"` checks; accounting-core currency types widened); P3 ✅ (6/11, `rateCardToBilling` now carries the rate matrix's currency + locked exchange_rate into billing items instead of hardcoded forex_rate:1 — non-PHP contract billings convert correctly through to GL; rest of chain already wired: UnifiedBillingsTab stamps base_amount, InvoiceBuilder converts per line, GL posting reads invoice rate). 28 tests, build clean, zero net new TS errors); P4 ✅ (6/11, FX revaluation generalized off USD — `useFxRevaluation` revalues all open foreign positions `original_currency != 'PHP'`, carries each record's real currency, resolves a period-end rate per distinct currency with a hard stop if missing; compute + JE builder were already currency-agnostic so one JE per date covers all currencies; panel copy/headers generalized). build clean, zero net new TS errors. NOT on prod.** P5 ✅ (6/11, verification): SPOT path verified SOUND — `autofillBillingsFromProject` is DEAD CODE (no callers); the live path (UnifiedBillingsTab → `send_billing_items_to_booking` RPC) carries currency+rate and HARD-BLOCKS non-PHP rows missing a rate, so no silent rate-1 (the only broken propagation was the contract generator, fixed in P3). Bulletproof logic test added (`multicurrency.integration.test.ts`, 7 cases: EUR/CNY conversion, FX metadata, glyphs, single + mixed-currency revaluation → one balanced + reversing JE). DB end-to-end on dev: EUR invoice + balanced EUR JE accepted by the FK gate, reval query picks up the open EUR invoice (test rows cleaned). FxRevaluationPanel mounted in the General Journal header (was unreachable — no entry point). tsc 159 (zero new), build clean. **ALL PHASES DONE + VERIFIED ON DEV. NOT on prod.** Prod release (when Marcus says go): apply migration 194 + seed period-end EUR/CNY rates + frontend (dev→main). Minor follow-up: NewJournalEntryScreen/CollectionCreatorPanel/AddRequestForPaymentPanel still map the static 4-currency list (work for seeded PHP/USD/EUR/CNY; switch to the hook for a 5th). |
| NEU-028 | Buying Price charges don't auto-pass to Selling Price (manual "Direct Entries") | Pricing / Quotations | P2 | Done (prod 6/11) | Reported 6/11 (Marcus). When you type charges directly into the **Buying Price** "Direct Entries" section, they don't flow into the **Selling Price** (selling Cost stays 0). Diagnosis: `convertBuyingToSelling` (`QuotationBuilderV3.tsx:1647`) correctly maps a buying category → selling category (`base_cost = buying price`, `amount_added/percentage_added = 0`, `final_price = base_cost`) — but it is **only called from the vendor-charge import handler** (`:1806` new-format, `:1871` old-format), i.e. only when charges are imported FROM a Vendor. Manually-entered buying lines never trigger it. **Open Q (UX decision — Marcus's call):** (a) real-time **auto-sync** buying→selling as you edit Buying (risk: clobbers any selling-side markups/edits already made — needs a merge that preserves existing markup), or (b) an explicit **"Copy Buying → Selling"** button (safer, user-initiated), or (c) sync-on-add only for brand-new buying lines. Recommend **(b)** a button + the existing `mergeSellingPriceCategories` to avoid wiping markup. Foundational data is all there; this is about *when* the copy fires. **DONE on dev 6/11 (Option B):** added a "Copy to Selling →" button to the Buying Price header (`BuyingPriceSectionV2`) → `handleCopyBuyingToSelling` in `QuotationBuilderV3` reuses `convertBuyingToSelling` (base_cost=buying, 0 markup) + `mergeSellingPriceCategories`, deduping by line id so repeat clicks are safe and existing selling-side markups are preserved. tsc 159 (zero new), build clean. NOT on prod. |
| NEU-029 | Selling Price: add a Profit Margin subtotal (₱ amount + %) | Pricing / Quotations | P3 | Done (prod 6/11) | Reported 6/11 (Marcus). The Selling section shows only "Subtotal (Selling Price)"; add a **Profit Margin** subtotal derived from the per-line markups — both the **₱ amount** (sum of `amount_added` across selling lines, incl. the % markups resolved to ₱) and the **%**. Data exists per line (`amount_added`, `percentage_added`, `base_cost`, `final_price`). Rendered in the Selling section footer (alongside Subtotal) — `SellingPriceSection.tsx` / `FinancialSummaryPanel.tsx`. **Open Q (define the %):** profit margin can mean (i) **markup on cost** = total profit ÷ total cost, or (ii) **margin on price** = total profit ÷ total selling price. Recommend showing **both labeled** (₱ profit, plus "X% markup" and "Y% margin") so it's unambiguous, or pick one — Marcus's call. Pure additive UI; no schema change. **DONE on dev 6/11 (your rec — both shown):** added a Profit Margin footer to the Selling section (`SellingPriceSection`) showing ₱ profit (Sell − Cost, PHP-converted totals) plus **"X% markup · Y% margin"** (markup = profit÷cost, margin = profit÷sell) and the Cost/Sell totals for context. tsc 159 (zero new), build clean. NOT on prod. |

| NEU-030 | Project detail tabs render BLANK for Pricing Supervisor (record visibility ON) | RBAC · Pricing / Projects | P1 | Done (prod 6/12) | Reported 6/11 (Falcons — Genevieve Lae S. Meria, `jr.supervisor06`, Pricing Supervisor, user `39bb11c2`). Opening any project (e.g. **ALL IN CIF / PRJ-504417**) shows the tab bar (Dashboard / Operations / Accounting / Collaboration) but **every tab's content area is blank** — no rows, no "access denied", just empty. Her project access + record visibility read as ON in the admin UI. **Root cause = the doctrine violation captured 6/11: "visible-but-not-explicit grants (UX-only cascade never materialized)."** `ProjectDetail.tsx:155-167` gates each tab on `can('pricing_projects_<tab>_tab','view')` (keys info/quotation/bookings/billings/invoices/collections/expenses/attachments/comments — `accessSchema.ts:919-933`); when a check is false the content is conditionally rendered as **nothing** (`:663-738`) → blank, not an error. The DB resolver (`152_resolve_from_assigned_profile.sql`) does an **exact-key lookup** against `users.access_profile_id → access_profiles.module_grants` and returns false if the key is literally absent. The admin toggle shows these child-tab grants as CASCADED/derived from a parent, but the save (`accessGrantUtils.ts resolvePerUserOverride`) persists only the **delta** — so the per-tab keys can appear checked in the UI yet were never written as explicit rows. (Note: "record visibility ON" is a SEPARATE dial — the read scope own/team/everything — from the per-tab module grant; having one ≠ having the other.) Three candidate exact causes, needs ONE read-only prod query to pin: (a) her `users.access_profile_id` is NULL; (b) points at an inactive/wrong profile (`ap.is_active=false`); (c) profile assigned but its `module_grants` JSONB is missing the `pricing_projects_*_tab:view` keys. **ROOT CAUSE CONFIRMED 6/12 (read-only prod queries) — and my first "NULL profile" theory was WRONG. The real cause is the cascade-not-materialized doctrine bug, and the Access Profile is irrelevant.** (1) The LIVE prod resolver `current_user_effective_module_grant` reads the **per-user Access Configuration FIRST** (`permission_overrides`, key present → wins true/false), and only falls back to the assigned Access Profile as a base template — so a NULL `access_profile_id` is NOT the cause (an earlier superseded migration-152 made me think the DB ignored overrides; the live function does not). (2) Genevieve's actual stored Access Configuration grants the PARENT `pricing_projects:view = true` (so she can open a project) but is **MISSING every per-tab child VIEW key** — `pricing_projects_info_tab:view`, `_quotation_tab:view`, `_bookings_tab:view`, `_billings_tab:view`, etc. are all ABSENT. `ProjectDetail.tsx:155-167` gates each tab on the **child** key → absent → resolver returns false → tab renders blank. **Smoking-gun of the materialization bug:** her config contains `pricing_projects_quotation_tab:edit = true` with **no matching `:view`** — an impossible/incoherent state that can only exist because the write-time cascade (parent-view ⇒ child-view; edit ⇒ view) was never materialized into the stored config. The admin UI shows the parent as granted (looks like she has Projects access) but the explicit child `:view` rows the runtime needs were never written. This is EXACTLY the doctrine in `feedback_rbac_explicit_visible_grants`: explicit + visible + enforced identically, **cascade must be materialized at write time** — and it wasn't. **Fix = (a)** write-time cascade in the admin Access Configuration save path (`accessGrantUtils.ts`): granting a parent module view (or any child action like edit) must materialize the implied child `:view` keys as explicit rows; **(b)** heal existing broken configs — backfill the missing `pricing_projects_*_tab:view` (and equivalent) keys for every user whose parent is granted but child views are absent (blast radius = anyone configured through the same un-materialized path, NOT the NULL-profile set, which was a red herring for this bug). Optional UX: honest empty-state instead of silent blank when a tab key is denied. **EXACT MECHANISM PINNED 6/12 (read-only): the cascade is materialized in exactly ONE writer — the admin UI save (`AccessConfiguration.tsx:356` → `resolvePerUserOverride` → `resolveCascadedGrants`, `accessGrantUtils.ts:37-90`). There is NO server-side cascade: prod has only `set_updated_at` triggers on `permission_overrides`/`access_profiles` (verified via information_schema.triggers) and the resolver does exact-key lookup. So EVERY other writer — the seed migrations that provisioned the Falcons team (172-181), the snapshot/clone tool, the `create-user` edge fn — writes raw parent grants WITHOUT expanding child tab keys. Genevieve's row was provisioned that way (parent `pricing_projects:view=true` stored, all child `*_tab:view` absent) and never re-saved through the UI, so her tabs were never materialized. The admin grid re-derives the cascade for DISPLAY (renders children checked) → looks granted, but storage has nothing → resolver denies → blank. Three layers, one cascades on display, one cascades on save (UI only), the DB does neither. **Long-term fix = move cascade materialization to the DB write layer: a BEFORE INSERT/UPDATE trigger on `permission_overrides` + `access_profiles` that expands parent→child (mirrors the retired migration-137 hidden-grant pattern) so ALL writers produce fully-materialized rows, + a one-time backfill to heal existing rows. Then grid/storage/resolver can never disagree again.** Core of the NEU-012 "materialize-at-write-time" principle. **IMPLEMENTED on dev 6/12 (migration `198_grant_cascade_materialization.sql`, generated by `scripts/genGrantCascadeMigration.ts` from ACCESS_SCHEMA — 539 edges):** (1) `access_cascade_edges` table (parent_key→child_key, generated, no drift), (2) `materialize_grant_cascade(jsonb)` pure fn that fills ABSENT child grants from their parent (explicit child true/false always wins), (3) BEFORE INSERT/UPDATE triggers on `permission_overrides` + `access_profiles` so EVERY writer materializes, (4) one-time backfill. **Verified on dev:** Genevieve's stored override went 273→554 keys, `pricing_projects_info_tab:view`/`bookings_tab:view` now stored=true; dry-run showed 0 rogue keys (every added key traces to a schema edge); trigger proven (insert profile w/ 1 parent key → auto-expands to 13); 8 Pricing override rows healed; tsc 159 (zero new). Applied to dev via `clone_exec_sql` (NOT recorded in dev migration ledger — the file IS the artifact; **must apply to prod at release**, where the same broken rows exist since dev is a prod clone). **Remaining (optional polish, deferred):** honest empty-state in `ProjectDetail.tsx` so a genuinely-denied tab says "no access" instead of rendering blank. **RELEASED TO PROD 6/12 (stable/2026-06-12):** migration 198 applied to prod (table+fn+triggers via apply_migration; 539 edges copied dev→prod via the table API; backfill run). Verified on prod: 539 edges, Genevieve's `pricing_projects_info_tab:view`/`pricing_projects_bookings_tab:create` now materialized, 8 Pricing rows healed. Done on prod+dev. **Honest empty-state DONE on dev 6/12 (full auto):** `ProjectDetail.tsx` now renders a "🔒 You don't have access to this tab — ask your admin to grant it in your Access Profile" message when the active tab's view grant is denied, instead of a silent blank. Also fixed a related minor bug: `ConsigneePicker.tsx` `resolve_by_name` queryFn returned `undefined` for a missing/deleted customer → React-Query "data cannot be undefined" throw → disabled consignee picker (a NEU-026 orphan symptom); now returns `null`. tsc 159 (zero new). Both NOT on prod (frontend — ship next release). |

| NEU-031 | Quotation "Mark as Approved" gated by department hardcoding, not access config | Pricing / Quotations · RBAC | P1 | Todo | Reported 6/15 (Marcus, from the prod diagnostic — "no Mark as approved in Jayson's account"). The quotation "Mark as Approved" action is hidden for Pricing because it's gated on `canActAsBD = can("bd_inquiries","edit")` (`src/components/pricing/StatusChangeButton.tsx:22`, used at `:183-194`) — a department-shaped check, NOT the access config. **Marcus's directive: actions should be gated on whether the user has the grant, not hardcoded to a department.** Fix: introduce a proper grantable action for quotation status changes (e.g. a quotation-approve/status capability in `ACCESS_SCHEMA`) and gate `StatusChangeButton` on `can(<module>, <action>)` instead of `bd_inquiries:edit`; assign it to the roles that should approve. Direct slice of NEU-012 (no hardcoded dept/role gates). Confirm the exact module/action key with Marcus before coding. |
| NEU-032 | Remove per-line converted (≈₱) markers on quotation — keep converted total | Pricing / Quotations · Documents | P2 | Done (prod) 6/15 | Reported 6/15 (Marcus, refining "remove the converted amount below the total"). **Decision: remove the per-line `≈ ₱` converted markers; KEEP the converted amount under the grand total.** Per-line markers render at `src/components/shared/pricing/UniversalPricingRow.tsx:494-507`. The converted TOTAL line (`PrintableDocumentHtml.tsx:229-233`, `PrintableDocumentPdf.tsx:535-539`, sourced `quotationDocumentResolver.ts:1280-1283`) STAYS. **CONFIRMED 6/15 (Marcus): "remove perline BUT keep currency marker at the very total."** **DONE on dev 6/15:** removed the per-line `≈ ₱` `<span>` (`UniversalPricingRow.tsx:494-507`) + its now-orphan `phpEquivalent`/`rate` locals (455-456). The converted TOTAL line in the print doc is untouched. The view-mode `DualCurrencyAmount` (the structured dual-currency total, not a "marker") left as-is. tsc 154 (zero new), build clean. **RELEASED TO PROD 6/15 (stable/2026-06-15-3, main @ a61b7a2).** **6/15 follow-up — the PDF still showed per-line currency.** Marcus clarified (screenshot): the printed AMOUNT/Price columns showed each line in its own currency ($/₱). **DECISION: Option B — keep original line values + keep the "Cur" column; show the currency SYMBOL only at the subtotal/grand total, per-line cells show the number only.** **DONE on dev 6/15:** in both renderers the per-line money cell currency is now `undefined` (`PrintableDocumentPdf.tsx:473`, `PrintableDocumentHtml.tsx:160`), so `formatMoneyValue` prints the number with 2 decimals and no symbol; the "Cur" column (`quotationDocumentResolver.ts:758`, USD/PHP) stays; group subtotals (503/189) + grand total (531) + the "≈" converted total (537) still pass currency → keep symbols. **6/15 — mixed-currency conversion breakdown added (Marcus's spec).** Since per-line symbols are gone, a mixed-currency quote needs the conversion legible at the totals. Single-foreign quotes keep the existing "≈ ₱[total]" line; MIXED quotes (>1 non-zero currency, totalled in PHP) now show a per-currency breakdown under the grand total, e.g. `incl. USD 1,203.00 ≈ ₱58,947.00 @ ₱49.00` (one grey line per foreign currency). Built via new `PrintableConversionLine` type + `PrintableTotals.conversions`; `summarizePricingCurrency` accumulates per-currency original+PHP sums (rate = php/original); rendered in both `PrintableDocumentPdf`/`Html` TotalsBlock. tsc 154 (zero new), build clean. **RELEASED TO PROD 6/15 (stable/2026-06-15-4, main @ 458a954).** Fully done on prod+dev. |
| NEU-033 | Vendor type → "International Partners" / "Local Partners" only | Pricing / Vendors | P1 | Todo | Reported 6/15 (Marcus — "make it International Partners and Local Partners only, we have local vendors too"). The master vendor "type" is hardcoded to 3 wrong options (`international` / `co-loader` / `all-in`) with no "Local Partners" anywhere. No DB enum — plain `service_providers.provider_type text`. Edit hardcoded options in: `PartnerSheet.tsx:311-315` (create) + TS union/defaults `:19,48,108,126`; `VendorDetail.tsx:500-504` (edit); `NetworkPartnersModule.tsx:133-135,153-156,497` (tab counts/filters); `VendorsSection.tsx:66,250-253,501-503` (quotation vendor picker); `useNetworkPartners.ts:13,42` (mapping + default). **Data backfill needed:** prod `provider_type` = international(139) / all-in(8) / co-loader(4) → remap the 12 all-in/co-loader rows (decision: → "local"?). Distinct from NEU-011 (that was persistence; this is the option set). |
| NEU-034 | Account owner blank in quotations dashboard | Pricing / Quotations | P2 | Done (prod) 6/15 | Reported 6/15 (Marcus — "account owner not displayed in the quotation dashboard"; "Let's fully fix this"). The "ACCOUNT OWNER" column exists (`QuotationsListWithFilters.tsx:1074` header, `:251-262` cell, `:1208` bound to `created_by_name`) but reads only the denormalized `quotations.created_by_name`, which is empty on **159 of 182 prod rows** — the save path never writes it (`QuotationBuilderV3.tsx:2310` sets `created_by` uuid only) and the fetch never joins users (`Pricing.tsx:69` `select('*')`). Fix: in the `Pricing.tsx` fetch (~68-88) collect distinct `created_by` uuids → fetch `users(id,name)` → fill `created_by_name ||= map[created_by]` (mirror the existing `pricingUserMap`, `QuotationsListWithFilters.tsx:372`). Covers historical rows; optionally also write `created_by_name` on save for future rows. **DONE on dev 6/15:** added a backfill in the `Pricing.tsx` quotations `queryFn` (after `merged`) — collects distinct `created_by` for rows missing `created_by_name`, fetches `users(id,name)` via `.in('id', …)`, fills `created_by_name`. Covers all historical rows; the column (`QuotationsListWithFilters.tsx:1208`) now resolves. tsc 154 (zero new), build clean. **RELEASED TO PROD 6/15 (stable/2026-06-15-3, main @ a61b7a2).** |
| NEU-035 | E-Voucher approval: under-configured routing + empty-looking approver page | Accounting / E-Voucher · RBAC | P1 | Todo | Reported 6/15 (Marcus — "e-voucher routed for approval in Jayson's account not reflected"; pushed back on the "already fixed" call: "it doesn't work yet, it's just the forwarding stuff"). **Re-diagnosed (read-only prod): approval + RLS WORK** — migration 208 fixed visibility; Jayson can see & approve routed vouchers and DID approve EV-2026-0030 today (→ pending_ceo). Two real issues: **(1) Routing under-configured (config, not code).** Prod `routing_rules` has exactly ONE row: `trigger {booking_service_type:"Forwarding"} → authority {dept:Pricing, role:manager}` (priority 10). Only structured, booking-linked Forwarding EVs auto-route to Jayson. A Forwarding-job expense filed via "New Request" with no booking link → `booking_id=null` → `booking_service_type=null` → no match → DB trigger `set_evoucher_pending_approver` defaults it to the requestor's own dept (Operations). The 2 booking-less EVs in his queue (EV-2026-0062/0087) were **hand-corrected via SQL today**, not by the engine. **DECISION 6/15 (Marcus):** keep the Forwarding rule exactly as-is — NO new rule. "All other expenses filed by Pricing staff → Jayson" is ALREADY the default behavior: the BEFORE INSERT trigger `set_evoucher_pending_approver` sets `pending_approver_department = requestor_department`, so a Pricing-staff expense (any type) defaults to the Pricing manager's queue = Jayson. The Forwarding rule only exists to OVERRIDE that default (forwarding-job expenses are often filed by Operations → rule yanks them to Pricing). So routing needs ZERO code/config change — just VERIFY on prod that a Pricing-staff non-forwarding expense reflects in Jayson's queue. Deliberately NOT covered: an Operations-filed forwarding-job expense with no booking link stays with Operations ("keep forwarding only") — separate future fix = require a booking link on New Request. **Remaining NEU-035 work = the approver-page UI only** (the all-zero tabs under the approval panel make it LOOK broken). **(2) Approver-page UX (the "looks broken" symptom).** For a pure approver who files no EVs, the tab counts (All/Draft/Pending/Active/Done) read 0 + "No expense requests yet" — they query `created_by=me` (`useEVouchers.ts:23-24`, scope defaults "mine", `MyEVouchersPage.tsx:60-66`) — shown directly under the populated "NEEDS YOUR APPROVAL (3)" panel (`MyEVouchersPage.tsx:100-105`, separate query). Fix: default scope to Dept (or surface the approval count in the tab strip) for users with `my_evouchers:approve`. Files: `routing_rules`, `src/utils/routing/resolveRouting.ts`, `useEVoucherSubmit.ts:218-238`, `useEVouchers.ts:23-44`, `MyEVouchersPage.tsx`. |

## Done

| ID | Title | Area | Closed | Notes |
|----|-------|------|--------|-------|
| NEU-004 | Closing of tickets from Inbox | Inbox / Workflow | 2026-06-04 | Let users close/resolve workflow inbox items directly. Shipped via migration `131_inbox_close_tickets.sql` + inbox close UI. |
| NEU-003 | Multiple POD rate billing | Pricing / Billing | 2026-05-30 | Per-POD (port-of-discharge) contract rate cards. Each POD gets its own complete, standalone rate card (REPLACE model, not additive). Data: optional `pod_scope?: string[]` on `ContractRateMatrix` (undefined/empty = applies to all PODs = legacy default → zero migration). Engine: new `selectMatrixForPod()` (scoped POD wins → global fallback) wired into `calculateContractBilling` + `generateRateCardBillingItems`; bookings carry one POD. UX ("Idea 1"): one card with a header `POD:` selector — "Default rates — all other PODs" + each POD; picking a port seeds a copy of default in one step; reset folded into the dropdown menu. POD list sourced from each service form's `pods` field (NOT `contract_general_details.port_of_entry`, which isn't rendered in the builder). Trucking excluded from v1 (multi-line billing doesn't thread POD). Fix: booking POD lives under `pod_aod` for Brokerage — `BookingRateCardButton` now reads all field variants. No DB migration. Files: `types/pricing.ts`, `utils/contractRateEngine.ts`, `utils/rateCardToBilling.ts`, `quotations/ContractServiceRateGroup.tsx` (new), `quotations/ContractRateCardV2.tsx`, `quotations/QuotationBuilderV3.tsx`, `pricing/ContractDetailView.tsx`, `contracts/BookingRateCardButton.tsx` + `InlineRateCardSection.tsx` + `RateCalculationSheet.tsx`. |
| NEU-001 | Restrict duplicate MBL/MAWB on booking creation | Ops / Bookings | 2026-05-29 | Hard block, globally unique. Migration 124 adds partial unique index `bookings_unique_mbl_mawb` on `lower(btrim(details->>'mbl_mawb'))` (case/whitespace-insensitive; blank/draft MBLs exempt). `CreateForwardingBookingPanel` translates the 23505 violation into a friendly toast on both submit and save-draft paths. MBL/MAWB is filled by Ops after creation, so the guard fires at save, not at the create modal. Migration 125 scopes uniqueness to `(service_type, mbl_mawb)` — same MBL is allowed across services (e.g. a shipment's Forwarding + Brokerage bookings) but blocked within a service. Released to prod 2026-05-30 (124 + 125; prod had 3 duplicate-MBL groups cleaned up first). |
| NEU-005 | Countries for Vendor | Vendors | 2026-05-29 | Countries are name-only (no ISO), managed in Admin → Profiling → Countries. Migration 123 makes `profile_countries.iso_code` nullable; registry `country` entry keyed on `name` (fixes the previously-broken add). Vendor country dropdowns (`VendorDetail`, `PartnerSheet`, quotations `VendorsSection`) wired to `ProfileLookupCombobox`. `NetworkPartnersModule` filter now sources the managed list and uses a searchable, right-aligned `CustomDropdown` (added opt-in `searchable` + `align` props). Removed dead `COUNTRIES` export. ⚠️ Migration 123 is dev-only — apply to prod on release. |
| NEU-002 | Attachment support for E-Voucher request | Accounting / E-Voucher | 2026-05-29 | File picker in `AddRequestForPaymentPanel`, upload via `uploadCrmAttachments` to `attachments` bucket, persisted to `evouchers.attachments` JSONB. Openable links in `EVoucherDetailView`. No migration needed. |

---

### Conventions
- **ID:** `NEU-###`, incrementing. Never reuse a number.
- **Area:** module/domain — Sales, Pricing, Contracts, Ops, Accounting, HR, RBAC, Catalog, Inbox, Vendors, Infra.
- Move to **Done** when merged; note the commit/PR.
- One ticket = one shippable change. Split anything larger.
- Open questions live in the Notes cell until resolved.
