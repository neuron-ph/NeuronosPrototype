node_modules
build/
dist/

# Claude Code / dev environment
.claude/
.mcp.json
.serena/
.dora/
.agents/
.impeccable.md
skills-lock.json
vite-qa*.log

# Local env — never commit secrets
.env.local
.env*.local

# QA artifacts

# Vercel (keep project.json for linking, ignore local build output)
.vercel/output/

.vercel

# Netlify build output / functions cache
.netlify/

# Marketing showcase — separate sub-workspace, not part of the main app
marketing-showcase/

# Local junk (do not commit)
image.png
UsersMarcus.claudesettings.json

# CRM analysis — contains real customer data, keep local
analysis/

# Playwright
/test-results/
/playwright-report/
/.last-run.json
